# Snow & Ice Animation ❄️

A simple JavaScript project that creates falling snowflakes and ice shards animation using HTML5 Canvas.

## 🚀 Features
- Pure JavaScript (no external libraries)
- Snowflakes and ice shards fall from top to bottom
- Randomized positions, speeds, and sizes
- Configurable parameters via `config.yml`

## 📦 Run
Open `index.html` in your browser.

## 🛠 Config
Animation parameters are stored in `config.yml`.

## 📊 Example
![Snow Animation Screenshot](example.png)
